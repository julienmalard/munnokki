[project]
name = "munnokki"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "appdata>=2.2.1",
    "numpy>=2.5.1",
    "xarray>=2026.7.0",
]


[tool.uv]
required-environments = [
  "python_version >= '3.14'",
]
